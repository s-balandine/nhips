AFTER RECORD
  IF (Q1001 = .) THEN
    HELP "RecID must be entered" TYPE=ERROR
    GOTO q1001
    EXIT
  ENDIF
  IF (Q1001 = .) THEN
    HELP "Firstname must be entered" TYPE=ERROR
    GOTO q1001
    EXIT
  ENDIF
  IF (Q1002 = .) THEN
    HELP "Lastname must be entered" TYPE=ERROR
    GOTO q1002
    EXIT
  ENDIF
END

recid
  KEY UNIQUE 1
  NOENTER
END

Q1001
  MUSTENTER
  AFTER ENTRY
    LET RECID=COPY(Q1001, 1, 1)+". "+Q1002
  END
END

Q1002
  MUSTENTER
  AFTER ENTRY
    LET RECID=COPY(Q1001, 1, 1)+". "+Q1002
  END
END

